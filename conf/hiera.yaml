---
:backends: yaml
:yaml:
  :datadir: "/tmp/shinesolutions/packer-aem/hieradata"
:hierarchy:
  - local
  - "%{::os.family}/%{::os.name}-%{::os.release.major}"
  - "%{::os.family}/%{::os.name}"
  # - author-publish-dispatcher # temporary because packer_build_name doesn't work
  - "%{::packer_build_name}"
  - serverspec
  - common
:logger: noop
:merge_behavior: native
:deep_merge_options: {}
