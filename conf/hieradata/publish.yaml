---
publish::aem_quickstart_source: "%{hiera('aem_quickstart_source')}"
publish::aem_license_source: "%{hiera('aem_license_source')}"
publish::aem_artifacts_base: "%{hiera('aem_artifacts_base')}"
publish::aem_healthcheck_version: "%{hiera('aem_healthcheck_version')}"
# TODO: how do we load a boolean value? "%{hiera('aem_sample_content')}"
publish::aem_sample_content: false
publish::tmp_dir: "%{hiera('tmp_dir')}"
publish::aem_repo_mount_point: /mnt/ebs1
aem_base::aem_healthcheck_version: "%{hiera('aem_healthcheck_version')}"
aem_base::aem_password_reset_version: "%{hiera('aem_password_reset_version')}"
aem_base::tmp_dir: "%{hiera('tmp_dir')}"
aem_base::aem_repo_device: /dev/xvdb
aem_base::aem_repo_mount_point: /mnt/ebs1
