{
  "aws_user": "{{ aws.user }}",
  "aws_region": "{{ aws.region }}",
  "profile": "{{ aem.profile }}",
  "source-ami": "{{ aws.source_ami }}",
  "iam_instance_profile": "{{ aws.instance_profile }}",
  "http_proxy": "{{ http_proxy }}",
  "https_proxy": "{{ https_proxy }}"
}
