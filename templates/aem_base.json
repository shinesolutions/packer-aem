{
  "variables": {
    "aws_region": "ap-southeast-2",
    "aws_user": "ec2-user",
    "iam_instance_profile": "",
    "aws_vpc_id": "",
    "aws_subnet_id": "",
    "aws_security_group_id": "",
    "component": "aem_base",
    "version": "x.x.x",
    "aem_base_instance_type": "m3.large",
    "aem_base_ami_source_ami": "ami-xxxxxxxx",
    "var_file": "conf/template-vars.json",
    "aem_quickstart_source": "",
    "aem_quickstart_destination": "/tmp/cq-quickstart.jar",
    "puppet_bin_dir": "/opt/puppetlabs/bin",
    "owner": "",
    "cost_center": "",
    "ami_users": ""
  },
  "builders": [
    {
      "name": "aem_base",
      "type": "amazon-ebs",
      "ssh_username": "{{user `aws_user`}}",
      "ami_description": "Packer AEM Bootstrap - AEM Base AMI ({{user `version`}})",
      "ami_name": "{{user `component`}}-{{user `version`}}-{{timestamp}}",
      "instance_type": "{{user `aem_base_instance_type`}}",
      "region": "{{user `aws_region`}}",
      "iam_instance_profile": "{{ user `iam_instance_profile` }}",
      "vpc_id": "{{user `aws_vpc_id`}}",
      "subnet_id": "{{user `aws_subnet_id`}}",
      "security_group_id": "{{user `aws_security_group_id`}}",
      "ami_users": "{{user `ami_users`}}",
      "source_ami": "{{user `aem_base_ami_source_ami`}}",
      "run_tags": {
        "Application Id": "Adobe Experience Manager (AEM)",
        "Application Role": "AEM Base AMI",
        "Version": "{{user `version`}}",
        "Name": "Packer Builder - AEM Base AMI ({{user `version`}})",
        "Owner": "{{user `owner`}}",
        "Cost Center": "{{user `cost_center`}}"
      },
      "tags": {
        "Application Id": "Adobe Experience Manager (AEM)",
        "Application Role": "AEM Base AMI",
        "Version": "{{user `version`}}",
        "Name": "AEM Base AMI ({{user `version`}})",
        "Owner": "{{user `owner`}}",
        "Cost Center": "{{user `cost_center`}}"
      }
    }
  ],
  "provisioners": [
    {
      "type": "file",
      "source": "conf/hieradata",
      "destination": "/tmp"
    },
    {
      "type": "file",
      "source": "test/serverspec",
      "destination": "/tmp"
    },
    {
      "type": "file",
      "source": "{{user `aem_quickstart_source`}}",
      "destination": "{{user `aem_quickstart_destination`}}"
    },
    {
      "type": "puppet-masterless",
      "manifest_file": "provisioners/puppet/manifests/{{user `component`}}.pp",
      "hiera_config_path": "conf/hiera.yaml",
      "module_paths": [
        "modules",
        "provisioners/puppet/modules"
      ],
      "puppet_bin_dir": "{{user `puppet_bin_dir`}}"
    },
    {
      "type": "shell",
      "script": "provisioners/cleanup.sh"
    }
  ],
  "post-processors": [
    {
      "type": "json-updater",
      "ami_id": {
        "{{user `var_file`}}": [
          "author_ami_source_ami",
          "publish_ami_source_ami"
        ]
      }
    }
  ]
}
