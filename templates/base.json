{
  "builders": [
    {
      "type": "amazon-ebs",
      "ssh_username": "{{user `aws_user`}}",
      "ami_description": "Packer AEM bootstrap AMI {{user `component`}}-{{user `base_ami_version`}}-{{timestamp}}",
      "ami_name": "{{user `component`}}-{{user `base_ami_version`}}-{{timestamp}}",
      "instance_type": "t2.small",
      "region": "{{user `aws_region`}}",
      "source_ami": "{{user `base_ami_source_ami`}}",
      "tags": {
        "component": "{{user `component`}}",
        "version": "{{user `base_ami_version`}}"
      }
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "script": "provisioners/rhel7.sh"
    },
    {
      "type": "file",
      "source": "conf/hieradata",
      "destination": "/tmp"
    },
    {
      "type": "puppet-masterless",
      "manifest_file": "provisioners/{{user `component`}}.pp",
      "hiera_config_path": "conf/hiera.yaml",
      "module_paths": [
        "modules"
      ]
    },
    {
      "type": "file",
      "source": "test/serverspec",
      "destination": "/tmp"
    },
    {
      "type": "shell",
      "execute_command": "{{ .Vars }} '{{.Path}}' '{{user `component`}}'",
      "script": "provisioners/serverspec.sh"
    },
    {
      "type": "shell",
      "script": "provisioners/cleanup.sh"
    }
  ]
}
